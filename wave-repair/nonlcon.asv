function [c, ceq] = nonlcon(x, wc, phi_n)

%%得到
fre = x(1);
alpha = x(2);
tau = 1 / (sqrt(alpha) * fre);
w_up = 1 / tau;
c(1) = w_up - wc;
complex_wc = 

T = alpha * tau;
G_later = tf([tau, 1], [T, 1]);
figurename('迟后环节');
bode(G_later);
grid on

end

